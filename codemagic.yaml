workflows:
  web-build:
    name: Web Build
    max_build_duration: 30
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Web
        script: npm run build
    artifacts:
      - dist/  # هذا هو المجلد الذي ينتج عنه البناء عادة في مشاريع Vite
