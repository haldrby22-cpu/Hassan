workflows:
  web-build:
    name: Web Build Workflow
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Web
        script: npx expo export:web
    artifacts:
      - dist/**/*
