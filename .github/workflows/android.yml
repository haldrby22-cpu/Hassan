name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. تحديث Node.js إلى الإصدار المطلوب
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        run: npm install

      - name: Build APK
        run: |
          cd android
          # تنظيف ملفات Gradle التالفة
          rm -rf gradle/wrapper/gradle-wrapper.jar
          
          # فرض استخدام إصدار Gradle المتوافق (8.5)
          gradle wrapper --gradle-version 8.5
          
          # إعطاء صلاحيات التنفيذ
          chmod +x gradlew
          
          # بناء التطبيق
          ./gradlew assembleRelease --stacktrace
