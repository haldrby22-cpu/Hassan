name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        run: npm install

      - name: Build APK
        run: |
          cd android
          # إزالة أي ملفات gradle wrapper تالفة
          rm -rf gradle/wrapper/gradle-wrapper.jar
          
          # فرض استخدام إصدار 8.5 المتوافق وتوليد wrapper جديد
          gradle wrapper --gradle-version 8.5
          
          # منح صلاحية التنفيذ
          chmod +x gradlew
          
          # البناء مع استخدام خيارات التوافق
          ./gradlew assembleRelease --stacktrace
