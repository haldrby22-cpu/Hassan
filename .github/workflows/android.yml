name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        run: npm install

      - name: Build APK
        run: |
          # حذف أي ملفات gradle قديمة أو تالفة قد تسبب تعارضاً
          rm -rf android/.gradle
          rm -rf android/gradlew
          
          # الانتقال لمجلد الأندرويد واستخدام gradle النظام مباشرة
          cd android
          gradle wrapper
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/*.apk
